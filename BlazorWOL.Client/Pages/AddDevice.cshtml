@page "/addnew"
@inject Microsoft.AspNetCore.Blazor.Services.IUriHelper UriHelper
@inject DevicesService DevicesService

<DeviceForm Item="@(new Device())" OnClickOk="@OnClickOk" />

@functions {
  async Task OnClickOk(Device device) {
    await DevicesService.AddDeviceAsync(device);
    UriHelper.NavigateTo("/");
  }
}